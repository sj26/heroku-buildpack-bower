#!/usr/bin/env bash

BUILD_DIR=$1
CACHE_DIR=$2

cd $BUILD_DIR

PATH="$BUILD_DIR/vendor/node/bin:$BUILD_DIR/bin:$BUILD_DIR/node_modules/.bin:$PATH" bower install \
  --config.storage.packages="$CACHE_DIR/bower/packages" \
  --config.storage.registry="$CACHE_DIR/bower/registry"
